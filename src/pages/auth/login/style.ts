import { FlexColumn } from "@/styles/flex";
import styled from "styled-components";

export const Form = styled.form`
  width: 59.1rem;
  ${FlexColumn};
  gap: 3rem;

  & > h2 {
    text-align: center;
  }
`;
